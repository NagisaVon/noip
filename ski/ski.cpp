#include <iostream>

using namespace std;

int mem[105][105]; // Max length from point (i, j)
int map[105][105];

int main() {
    int r, c, i, j;
    cin >> r >> c;
    for ( i = 0; i < r; i++ )
        for ( j = 0; j < c; j++ )
            cin >> map[i][j];
    for ( i = 0; i < r; i++ )
        for ( j = 0; j < c; j++ )
            bfs[]
    return 0;
}
